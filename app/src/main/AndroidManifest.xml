<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.ralsei.whatcanyousee">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MazeLibrarianActivity"></activity>
        <activity
            android:name=".ChooseGameActivity"
            android:label="@string/title_activity_choose_game"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivity2"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GameLostActivity" />
        <activity
            android:name=".GameWonActivity"
            android:label="@string/title_activity_game_won"
            android:parentActivityName=".MazeActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="ru.ralsei.whatcanyousee.MazeActivity" />
        </activity>
        <activity android:name=".MazeActivity" />
    </application>

</manifest>